name: Test Runner Nest

on:
  push:
    branches: [dev]

jobs:
  test:
    runs-on: [self-hosted, nest]
    steps:
      - name: Confirmar runner
        run: echo "Runner nest funcionando en $env:COMPUTERNAME"
        shell: powershell